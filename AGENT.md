# PPT 项目规则

## 可读性与字号

- 设计画布为 1440 × 810（16:9）。所有新增或修改的、面向观众的正文文字最小为 **12px**。
- 技术卡片、流程图等密集模块中，供阅读的说明文字仍应为 **12px**；短标签、编号或英文缩写可为 **11px**。
- 资料来源、脚注、口径说明等辅助文字最小为 **10px**；不得低于此值。
- 不要通过缩小文字来塞入更多内容。文字超出时，应精简文案、拆分页面或调整信息结构。
- 图片、视频、截图中原本包含的文字不适用上述字号规则；但应确保其在播放尺寸下仍能辨认。

## 页面维护

- B–G 页面分别维护在 `slides/` 对应板块文件中；C 为模型与工具板块。保持页面 ID 稳定。
- 修改页面后运行 `tests/test_composer.py` 的完整离线浏览器验证；涉及版式时检查播放器中的实际阅读效果。
- 保留 `tests/test_composer.py` 中的字号下限回归校验：播放页 CSS 的显式字号不得低于 **10px**。
- 本项目是静态 `file://` 页面，不提供热更新。修改 CSS 或 JavaScript 后，退出播放器并按 **⌘R** 刷新页面，再重新打开受影响页面验证。

## 版本管理

- 每个任务完成并通过相应验证后，创建一次 Git 提交，便于后续回溯和版本修改管理。
- 提交前确认暂存区只包含当前任务相关的文件；工作区已有的无关改动必须保留且不得一并提交。
- 提交信息应简明描述本次任务的实际变更；未完成或未通过验证的工作不得提交。

## 源材料隔离

- `source/` 只作为制作输入，不能成为 `index.html`、`slides/`、样式或运行时素材的依赖；交付后删除整个 `source/` 目录，当前演示稿仍必须完整运行。
- 复用源材料页面时，将 HTML、必要样式、图片和视频复制到当前运行资产（如 `slides/`、`assets/`）中。页面原本是图片或视频时可复制该素材；可编辑页面应保留可编辑的 HTML，排版不适配时重写。
- 每次引入源材料后，使用浏览器验证 G 板块缩略图、播放器和 `file://` 运行均不请求或引用 `source/`。

## `resource/` 参考目录隔离

- `resource/` 仅作人工查阅和内容对比的参考源，不得作为实现产物，也不得直接修改其中的材料来支持页面运行。
- `index.html`、`slides/`、JavaScript、CSS、iframe `src` 及其他运行时资源不得直接引用或请求 `resource/`；禁止把 `resource/` 下的文件作为运行时依赖。
- 需要复用 `resource/` 内容时，必须将 HTML、必要样式、图片和视频复制到项目内受控的运行资产目录（如 `slides/`、`assets/`）后再接入；后续修改只作用于项目内副本。
- 每次接入或替换后，使用浏览器和静态检查确认播放器、缩略图及 `file://` 运行不请求、不引用 `resource/`。
